name: Deploy to Hugging Face Space

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          lfs: true # <--- FIX 1: Forces runner to download the actual 440MB binary model

      - name: Prepare Production Package
        run: |
          mkdir ../deploy_dir
          
          cat << 'EOF' > ../deploy_dir/README.md
          ---
          title: Phishing Detection API
          emoji: 🛡️
          colorFrom: blue
          colorTo: indigo
          sdk: docker
          pinned: false
          ---

          # PhishGuard MVP API
          This backend powers the Phishing Detection application.

          EOF
          
          cat README.md >> ../deploy_dir/README.md
          
          # Copy essential files
          cp Dockerfile ../deploy_dir/
          cp requirements.txt ../deploy_dir/
          cp main.py ../deploy_dir/
          cp predictor.py ../deploy_dir/
          cp .gitattributes ../deploy_dir/ # <--- FIX 2: Tells HF this contains LFS files
          
          mkdir -p ../deploy_dir/04_Saved_Models
          cp -r 04_Saved_Models/* ../deploy_dir/04_Saved_Models/

      - name: Push Single Commit to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          cd ../deploy_dir
          
          git init -b main
          git config user.email "action@github.com"
          git config user.name "GitHub Action Bot"
          
          git add .
          git commit -m "deploy: resolve LFS pointer bug and add bs4"
          
          git remote add hf https://user:$HF_TOKEN@huggingface.co/spaces/ab2403/phishguard-api.git
          git push hf main --force